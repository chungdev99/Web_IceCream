import React from 'react';
import PropTypes from 'prop-types';

LoadingProduct.propTypes = {
    
};

function LoadingProduct(props) {
    return (
        <div>
            1234
        </div>
    );
}

export default LoadingProduct;